# scripts/

One-off local ingestion utilities used during initial data preparation.
These scripts are **not** part of the runtime pipeline (`src/`) and are
not required to run the application. They contain hardcoded local paths
specific to the original development machine.
